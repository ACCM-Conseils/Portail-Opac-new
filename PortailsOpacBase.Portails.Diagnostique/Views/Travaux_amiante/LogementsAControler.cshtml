@model List<diag_logement>

@using PortailsOpacBase.Portails.Diagnostique.Models

<table id="table_logements" class="table table-striped">
    <thead>
        <tr class="info">
            <th>Logements traîtés</th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (diag_logement l in Model)
        {
            <tr>
                <td>@l.gbal</td>
                <td>@l.appt</td>
                <td><input class="diag" type="checkbox" data-id="@l.id" data-gbal="@l.gbal" /></td>
            </tr>
        }
    </tbody>
</table>
